import { ResponsiveStyle } from '~lib/styled-components'

export const style: Record<string, ResponsiveStyle> = {
  home: {
    flexFlow: 'column',
    placeItems: 'center',
    rowGap: '2rem',
  },
}
